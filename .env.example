# Airline Manager 4 Bot Configuration
# Copy this file to .env and fill in your values

# ============================================
# Authentication (Required)
# ============================================
# SECURITY: Credentials are ONLY read from .env file!
# They are NOT stored in config.ts or any other file.
# Never commit your .env file to version control!
EMAIL=your-email@example.com
PASSWORD=your-password

# ============================================
# Fuel & CO2 Purchase Limits
# ============================================
# Maximum price thresholds for automated purchasing
# Bot will skip purchase if current price exceeds these values

MAX_FUEL_PRICE=550
MAX_CO2_PRICE=120

# ============================================
# Fleet Management Configuration
# ============================================
# Percentage of total fleet to depart per run
# Example: 0.10 = 10% of fleet, 0.25 = 25% of fleet
FLEET_PERCENTAGE=0.10

# Delay between plane operations (in milliseconds)
FLEET_MIN_DELAY=1000
FLEET_MAX_DELAY=2000

# Mock mode: Set to 'true' for testing without real departures
FLEET_MOCK_MODE=false

# GLOBAL MAXIMUM: Max departures per run (overrides percentage calculation)
# Leave empty or remove this line to use percentage-based calculation (recommended)
# Set to a number to override percentage calculation (useful for testing)
# Examples:
#   MAX_DEPARTURES_OVERRIDE=        (no override, uses 10% of fleet)
#   MAX_DEPARTURES_OVERRIDE=5       (override: max 5 departures per run)
#   MAX_DEPARTURES_OVERRIDE=2       (useful for timeout test with 60s limit)
# Note: Timeout test (npm run test:timeout:headed) respects this setting just like all other tests
MAX_DEPARTURES_OVERRIDE=

# ============================================
# Notifications (Optional - Future)
# ============================================
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token
